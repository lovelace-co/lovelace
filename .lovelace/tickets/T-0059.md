---
id: T-0059
type: task
status: in_review
created: 2026-07-04T21:51:03Z
updated: 2026-07-04T21:57:24Z
title: Core and host mutations for project settings (name, actors)
parent: E-0003
priority: high
---

## Description

Add the validated mutations Settings needs, following the writeWorkflow / writeAutomations node-surgery pattern (patch the YAML node in place, preserve the rest, validate before writing).

- writeManifest(root, { name }): patch manifest.yaml name in place, reindex.
- writeActors(root, actors): write actors.yaml. Enforce exactly one human and unique ids. Block removing an actor still referenced by a ticket assignee, session, or comment, with a clear message naming the count (like the workflow removal guard). Actor id rename is out of scope for now (would need a cascade into tickets, sessions and comments).
- Host ops write_manifest and write_actors; host client (lib/host.ts) and fake host (test/fakeHost.ts) methods.

## Acceptance criteria

- [ ] writeManifest renames the project; manifest comments and other keys are preserved.
- [ ] writeActors writes actors.yaml; rejects a second human and duplicate ids.
- [ ] Removing a referenced actor is blocked with a message naming the count.
- [ ] Core tests cover rename, write, single-human, and the referenced-actor block.
- [ ] Host op, host client and fake host wired; pnpm build green.
