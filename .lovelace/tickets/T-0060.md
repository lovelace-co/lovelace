---
id: T-0060
type: task
status: backlog
created: 2026-07-04T21:51:03Z
updated: 2026-07-04T21:51:03Z
title: Decompose the workflow editor into a shared draft hook and sections
parent: E-0003
priority: medium
---

## Description

Refactor Workflow.tsx so its editing state can back two Settings tabs that share one save, without a behaviour change.

- Extract useWorkflowDraft(snapshot, onSave): row state, transitions map, dirty, reseed, save, handlers.
- Split the render into StatusesSection (statuses table + transitions matrix) and TypesFieldsSection (types + priorities + FieldsEditor).
- The existing Workflow view is recomposed from these; tests stay green.

## Acceptance criteria

- [ ] useWorkflowDraft owns the workflow editing state; one dirty flag and one save span both sections.
- [ ] StatusesSection and TypesFieldsSection render the current editor content.
- [ ] Existing workflow tests pass, updated only for structure if needed.
