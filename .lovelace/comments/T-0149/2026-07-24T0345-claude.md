---
ticket: T-0149
actor: claude
created: 2026-07-24T03:45:56Z
---

The in-repo work is done (commit 1699994): `scripts/release/render-cask.mjs` renders a style-clean cask, `.github/workflows/homebrew-cask.yml` syncs the tap on every published release, and the readme documents the brew install. What remains needs a human, in this order:

1. Create the tap repository. The initial content (Casks/lovelace.rb for 0.1.0 with the real sha256s, plus a README) is staged as a committed git repo at `/var/folders/9d/kctzbbm92z32gslwdtpntgqm0000gn/T/tmp.WJUclyFn9D/homebrew-tap`. From that directory run: `gh repo create lovelace-co/homebrew-tap --public --description "Homebrew tap for the Lovelace desktop app" --source . --push`. If the temp directory is gone, regenerate the cask with `node scripts/release/render-cask.mjs 0.1.0 <arm_sha> <x64_sha>` (recompute the two sha256s from the v0.1.0 release DMGs with `shasum -a 256`).
2. Mint a fine-grained GitHub PAT with contents write permission on `lovelace-co/homebrew-tap` only (GitHub, Settings, Developer settings, Fine-grained tokens), then store it: `gh secret set HOMEBREW_TAP_TOKEN --repo lovelace-co/lovelace`. Without this secret the sync workflow fails by design, with an error naming the secret.
3. Prove the automation end to end: run the homebrew-cask workflow once by hand (Actions, homebrew-cask, Run workflow, tag `v0.1.0`) and confirm it reports "cask already current" or pushes an identical cask to the tap.
4. Verify the acceptance criteria on a Mac: `brew tap lovelace-co/tap && brew install --cask lovelace` installs to /Applications and the app launches past Gatekeeper; `brew uninstall --cask lovelace` removes it; `brew audit --cask lovelace` passes in the tap. Ideally on a machine (or fresh user account) without an existing manual install, then once over a manual install to confirm brew replaces it cleanly.
5. On the next real release (v0.1.1 or later), confirm the cask in the tap updated without manual editing; that closes the ticket's automation acceptance.

Session S-0148 carries the same list as open questions. The staged tap content and the exact commands were also handed over in the working conversation on 2026-07-24.
