---
id: S-0105
ticket: T-0079
actor: claude
started: 2026-07-12T08:26:44Z
ended: 2026-07-12T08:26:44Z
commits: []
outcome: partial
---

## Approach

Fix the schema editor's round-trip fidelity: rows carry the parsed definition they were seeded from and rebuild from it, so hand-authored non-enum defaults, redundant labels and explicit required false survive open and save; hide the locked title row by identity so a colliding rename stays visible. Programmer implements, I audit, a fresh-context verifier attacks byte fidelity end to end.

## What happened

The first round landed the settled design: StatusRow, FieldRow and TypeRow gained originalDef; buildStatuses, buildFields and buildTypes rebuild untouched rows byte-faithfully (authored label kept even when equal to Title Case, required false kept only when authored, non-enum defaults of any type passed through unless the field type changed) while edited and fresh rows keep the clean-defaults rules; core compactField now emits required whenever it is defined instead of collapsing an authored false; FieldsEditor hides the pinned locked row by identity instead of by machine name. The verifier confirmed all three acceptance criteria against a hostile hand-authored fixture (byte-identical no-op save, single-line diffs for unrelated edits, authored forms surviving renames alongside unknown keys, collision rows visible with core's duplicate rejection surfacing) and found two pre-existing neighbours inside the ticket's theme: an authored redundant plural is destroyed by any save and lights Save on open, and an unknown key on a field leaves the draft permanently dirty. A fix round for both is with the programmer now: the rebuildLabel pattern extended to plural, and dirty detection re-based on a seed-then-build projection so both sides of the comparison share the same canon.

## Open questions

- Fix round in flight for the two verifier neighbours (plural fidelity, dirty projection); audit and a focused verifier re-check pending before the ticket closes.
- Authored label of an empty string is dropped by core's truthiness gates on save; pathological hand-edit, accepted and noted rather than fixed.
- Non-canonical authored key order inside a field is canonicalised by any real edit; unreachable as a pure no-op, accepted.
