---
id: S-0086
ticket: T-0089
actor: claude
started: 2026-07-12T03:42:44Z
ended: 2026-07-12T03:42:44Z
commits: []
outcome: partial
---

## Approach

Delegated to a programmer subagent: remove the auto-populated template body from createTicket in packages/core/src/mutate.ts so a ticket created without a body is a clean frontmatter-only file, with byte-exact tests for the new shape, the explicit-body path and round-trip fidelity.

## What happened

Implemented as briefed: body defaults to empty, a body-less file is frontmatter plus a single trailing newline with no dangling blank line, explicit bodies unchanged. Three byte-exact tests added to packages/core/test/mutate.spec.ts with a red-first proof. The agent traced the three consumers: the indexer never reads bodies, the MCP create path passes no body, and TicketDetail already renders a No description yet placeholder for empty bodies. Orchestrator audited the diff (scoped exactly to the body block), verified an aborted git stash during the agent's red-first check left no damage to the concurrent session's uncommitted rename work, and re-ran the full pnpm test (380 passing) and pnpm build. Because this touches a core mutation path and the round-trip acceptance bar, a fresh-context verifier is now attacking the change (updateTicket on body-less files, validator, index determinism, whitespace-only bodies); the ticket stays in progress until its verdict.

## Open questions

- Verifier verdict pending on the empty-body round-trip, updateTicket on frontmatter-only files, validator acceptance and index determinism; the ticket moves to done only if it confirms.
- Existing tickets created before this change keep their placeholder bodies; nothing migrates them.
- No commits were made: the working tree carries a concurrent session's uncommitted schema rename in mutate.ts, so committing is left to the owner.
