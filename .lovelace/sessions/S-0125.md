---
id: S-0125
ticket: T-0129
actor: claude
started: 2026-07-17T10:14:41Z
ended: 2026-07-17T10:14:41Z
commits:
  - 1922a00
outcome: completed
---

## Approach

Implement T-0129 by delegating to three parallel programmer subagents on disjoint file sets, then audit everything myself: (A) scripts/release/sync-version.mjs with version:set and version:check, the APP_VERSION constant in packages/mcp, CI wiring and the RELEASING.md rewrite; (B) a packages/core test tying SPEC.md's three stated versions to SPEC_VERSION; (C) a quiet running-app-version footer in GeneralSettings via Tauri getVersion.

## What happened

All three subagents delivered as briefed. My audit re-ran every acceptance check first-hand: version:check clean on HEAD; version:set 0.2.0 touched exactly the eight declaration sites one line each; a tampered Cargo.lock was caught by name; malformed and prerelease versions refused; version:set 0.1.0 restored byte-identical files (verified by git hash-object); pnpm build green; the Bun sidecar compile succeeded with the APP_VERSION import, answering S-0124's open question. Desktop suite is 350/350 including the two new GeneralSettings tests; the new core spec-doc tests and mcp version test pass. Root pnpm test stays red for a pre-existing reason: the demo fixture gained T-0005 in commit 72fd60e without the hard-coded expectations moving (9 core failures, 2 mcp). I reproduced the identical failures on clean HEAD in a fresh worktree, so it is baseline breakage, filed as bug T-0130 (todo, high). Work committed as 1922a00; ticket moved to in_review with acceptance boxes ticked and an outcome note. Also resolved: the leftover demo-project state files from S-0120/S-0121 are gone.

## Open questions

- T-0130 (fixture drift: demo project has five tickets, tests expect four) keeps root pnpm test red until fixed; it also blocks T-0129's final acceptance box.
- A manual pass in pnpm app:dev to see the 'Lovelace 0.1.0' line in General settings would close the loop on the UI change; the rendering is covered by mocked tests only.
- The website repository's CURRENT_VERSION in src/consts.ts remains a manual promotion step; T-0117 owns folding it into the release checklist.
