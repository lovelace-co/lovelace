---
id: S-0085
ticket: T-0090
actor: claude
started: 2026-07-12T03:41:20Z
ended: 2026-07-12T03:41:20Z
commits: []
outcome: completed
---

## Approach

Scouted the cause directly (the new-tab button carries both btn-ghost and tab-new, so the shared tertiary capsule pseudo-element materialises on top of the tab strip's rectangular hover), then delegated the one-rule fix to a programmer subagent with the diagnosis in the brief.

## What happened

Added a single rule in apps/desktop/src/styles/app.css suppressing the capsule for this control only: .tab-new::before { content: none; }, placed after the shared .btn-ghost::before definition so it wins the cascade at equal specificity. The rectangular full-height hover and the colour change stay. All other btn-ghost consumers (bulk bar, modals, settings) are unaffected since no other element carries both classes. Orchestrator audited the diff and re-ran pnpm test and pnpm build; both pass.

## Open questions

- Worth a quick visual hover check in both themes; the change is CSS-only and jsdom cannot assert pseudo-elements.
- No commits were made: the working tree carries a concurrent session's uncommitted work in app.css, so committing was left to the owner.
