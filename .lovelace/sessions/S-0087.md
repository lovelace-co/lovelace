---
id: S-0087
ticket: T-0091
actor: claude
started: 2026-07-12T03:49:16Z
ended: 2026-07-12T03:49:16Z
commits: []
outcome: completed
---

## Approach

Orchestrated a scout to map the status-change paths, then one programmer subagent. Replaced the read-only status chip in the ticket detail Properties panel with the shared Dropdown control (options from schema.yaml statuses via statusLabel, in schema order), wired through the existing change() helper so a pick saves { status } through onUpdate and host updateTicket. Extended Dropdown with an allowEmpty prop (default true) so the locked status field offers no empty (none) choice; every other consumer is unchanged.

## What happened

Implementation landed cleanly in TicketDetail.tsx, Dropdown.tsx and ticketDetail.spec.tsx (five new tests: label display, full option list with no empty choice, save on pick, no-op on same pick, hand-edited unknown status still renders Title-Cased and selectable). Audited the diff against a baseline snapshot of the dirty tree; re-ran pnpm test and pnpm build myself, both green (desktop 218 then 217 after suite renames elsewhere; all suites passing).

## Open questions

- Work is uncommitted, in in_review for the owner visual pass.
- The status row now shows plain labels like the BulkActions move dropdown; the hue dot no longer appears in the detail Properties row. If the dot should survive there, Dropdown needs per-option adornment support.
