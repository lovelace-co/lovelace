---
id: S-0109
ticket: T-0118
actor: claude
started: 2026-07-12T09:05:52Z
ended: 2026-07-12T09:05:52Z
commits: []
outcome: completed
---

## Approach

Orchestrated (analysis by the session model, implementation by a programmer subagent, cold gate by a verifier subagent). Replaced the singleton state/presence.json live marker with per-session entries state/presence/&lt;session-id&gt;.json carrying a beat_at heartbeat: core gained writePresence/clearPresence/readPresences/beatPresence (manifest-only hot path, GC on the write path, legacy singleton read and swept), the helper gained presence-beat on PostToolUse and folded the Stop-time clear into session-check so a blocked stop keeps the ring, writeHooks migrates old installs, the snapshot carries the entry array, the app derives a per-ticket elapsed map, the Rust and chokidar watchers react to the presence directory, the default cap dropped 120 to 15 minutes, SPEC.md moved to 3.2.0 (SPEC_VERSION constant bumped to match) and ADR-0012 supersedes ADR-0009.

## What happened

Implementation landed cleanly and was audited: diff read in full, suites re-run, and the lifecycle attacked end-to-end against the compiled helper (interleaved sessions, blocked stop, legacy sweep, hostile session ids). A fresh-context verifier then refuted two claims: core watchProject's presence exceptions were dead code because chokidar prunes at the ignored state directory (pre-existing for the legacy file; the desktop app was unaffected since it watches via the Rust shell), and a plain file named state/presence crashed readPresences with ENOTDIR, making the project unopenable. Both were fixed with red-first regression tests. Final state: core 192/192, mcp 81/81, desktop 255/255, core and mcp builds clean, cargo check clean. Mid-work the presence directory itself showed three live sessions on this tree (this one, a concurrent session on T-0115, and a stale T-0113 entry correctly past the cap), which is the feature demonstrating itself. The T-0118 implementation subagent also accidentally ran git stash and immediately popped it; content was verified intact, but the pre-existing staged rename of workflow.yaml to schema.yaml now shows as a staged add plus unstaged delete.

## Open questions

- Nothing committed: the tree already stacks the owner's uncommitted work (schema rename, versioning tickets, and a concurrent session's update-pill work), so these changes join the stack for the owner to land.
- Sidecar binaries not rebuilt (pnpm sidecars); the packaged app ships stale presence behaviour until they are. Dev via pnpm app:dev picks up dist and is current.
- examples/demo-project manifest still declares spec_version 3.0.0 while SPEC.md is at 3.2.0; left for the in-flight versioning work to decide whether the fixture's declared floor should move.
- Verifier nits left as-is: a future beat_at (clock skew) keeps an entry alive until real time passes it; the writeHooks migration strips a degenerate Stop entry with an empty hooks array; gcPresence deletes garbage-timestamp entries that readPresences would display.
- The 15 minute default cap means a session waiting more than 15 minutes on a permission prompt (no tool calls, so no beats) goes dark while technically still open; judged acceptable, revisit if it annoys.
