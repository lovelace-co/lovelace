---
id: S-0090
ticket: T-0100
actor: claude
started: 2026-07-12T04:44:12Z
ended: 2026-07-12T04:44:12Z
commits: []
outcome: completed
---

## Approach

Centre the Board card lead slot (status dot and swap-in checkbox) on the title's first line and midway between the card edge and the text. Derived the geometry first (card padding 15px 18px, lead 18px, title 14px at line-height 1.5 giving a 21px first line, title left edge at 46px), then briefed the programmer subagent with exact values and audited the returned diff.

## What happened

CSS-only change in apps/desktop/src/styles/app.css: .ticket-card-top gap 10px to 14px, .card-lead gains margin-left -4px and margin-top becomes calc((var(--size-body) * 1.5 - 18px) / 2) (1.5px). The lead now spans 14..32px so its centre sits at 23px, the midpoint of the 46px gutter, while the title's left edge stays exactly at 46px; vertically the 18px slot centres on the 21px first line. The comment above .card-lead now records the centring intent. Audited: read the diff, confirmed .ticket-card-top and .card-lead are used only by Board.tsx, re-ran pnpm test (132 + 217 + 52 tests pass) and pnpm build (clean apart from the pre-existing chunk-size warning) myself.

## Open questions

- Work is uncommitted, in in_review for the owner visual pass; app.css also carries the earlier uncommitted schema-rename-era changes, so this hunk should land with or after that work.
- The .priority-dot base rule still carries margin-top: 7px, a leftover from when the dot sat directly in the card top row; every current consumer overrides it to 0, so it could be removed in a later tidy.
- The checkbox in checkbox mode now bleeds 4px into the card's left padding by design; if the owner finds it too close to the card edge at a glance, the gutter maths lives in the .card-lead comment.
