---
id: S-0098
ticket: T-0096
actor: claude
started: 2026-07-12T07:10:36Z
ended: 2026-07-12T07:10:36Z
commits: []
outcome: completed
---

## Approach

Complete the migration framework after the verifier's hostile-input findings: send the four defects and the plan-target mismatch back to the programmer for a fix round, audit the fixes, re-gate with the same verifier, and close its one residual finding myself.

## What happened

The fix round landed up-front workflow.yaml validation before any write (missing file, YAML errors with file and line, empty or missing types and statuses, non-array fields, applies_to targets checked against real type names), per-node unknown-key preservation mirroring withUnknownKeys with the consumed 2.x keys excluded, a single-pass role decision so plan and apply can never disagree on double-flagged statuses, and the plan target corrected to the chain's 3.0.0 floor. The verifier re-confirmed all five against its original fixtures and found one residual neighbour: element-level garbage (scalar entries where mappings belong) passed the array-level guards and still half-wrote. I closed that class myself: apply now writes schema.yaml, validates it through the real 3.0 parser (loadSchema plus validateSchema), and on any failure removes the generated file and refuses with a corrective message, leaving workflow.yaml and the manifest untouched; the verifier's exact repro is a regression test. Also noted: the verifier flagged absent-fields tolerance as a deviation, but that was a wording slip in my re-check message, not a defect; absent fields defaulting to an empty list is the designed behaviour. Final state: full suite green (core 171, desktop 228, mcp 58), build green, sidecars rebuilt with migration ops confirmed in the host binary. The whole ADR-0011 plan (T-0094, T-0095, T-0096) is now implemented and adversarially verified; nothing is committed per the owner's standing instruction.

## Open questions

- Nothing committed: the tree now stacks the schema rename, T-0098, T-0103 and all three versioning tickets; the owner decides when and how to land it.
- No lock guards two concurrent migrateProject calls on one tree; low risk, worth a framework doc note when migrations grow.
- The agent helper still lacks a top-level catch (raw stack trace on a gated project); small pre-existing ticket candidate from S-0094.
- plan() on element-level garbage prints odd lines before apply refuses cleanly; harmless, could pre-validate in plan too if it ever confuses anyone.
