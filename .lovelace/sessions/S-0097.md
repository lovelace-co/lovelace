---
id: S-0097
ticket: T-0096
actor: claude
started: 2026-07-12T06:51:23Z
ended: 2026-07-12T06:51:23Z
commits: []
outcome: partial
---

## Approach

Implement the ADR-0011 migration framework: a chained per-major step registry in core with plan and apply, host ops for both, the functional needs-migration screen in the app, and the 2.x to 3.0 reference step. Programmer implements to a brief, I audit, a fresh-context verifier attacks the result.

## What happened

First implementation round landed the full framework: migrations registry (floor at 2.x, already-current and too-new refusals, raw manifest read so planning works on a gated project), the v2-to-v3 document-transforming step (fields nested per applies_to, first active and complete statuses gain roles, no ready role invented, transitions and automations removed, manifest patched in place to the 3.0.0 floor), migration_plan and migrate_project host ops, and the SpecGate migration flow with plan list, commit-first note and one Update project action. My suite run caught a transient mcp test failure traced to the concurrent session rebuilding dist mid-run; three clean re-runs plus a clean full run confirmed (core 162, desktop 228, mcp 58). The verifier confirmed the happy path end to end (plan side-effect free by whole-tree hash, real 2.x project migrates to a clean deterministic 3.0 tree with tickets byte-untouched, generated schema judged more faithful than the historical hand migration) but found four hostile-input defects: raw TypeError crashes on malformed workflow.yaml, per-node unknown keys silently dropped, contradictory plan lines when a status carries both flags, and a field-deleting applies_to typo announced backwards, plus a misleading plan target of 3.1.0. A fix round covering all five, with up-front validation before any write, is with the programmer now.

## Open questions

- Fix round in flight: up-front workflow.yaml validation naming file and line, per-node unknown-key preservation, single-pass role decision, applies_to refusal, plan target as the chain floor; audit and verifier re-check pending.
- Sidecars not yet rebuilt for T-0094/95/96; rebuild once the fix round lands.
- No lock guards two concurrent migrateProject calls on one tree; acceptable for now, worth a note in the framework docs.
