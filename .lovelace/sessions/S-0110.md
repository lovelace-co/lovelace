---
id: S-0110
ticket: T-0119
actor: claude
started: 2026-07-12T09:06:04Z
ended: 2026-07-12T09:06:04Z
commits: []
outcome: completed
---

## Approach

Programmer subagent, audited by the session model. The Rust watcher's debounce channel now carries whether each event batch held a real change (any non-ignored, non-presence path); the emitted lovelace://changed payload gains presenceOnly, the host client forwards it (defaulting to false when absent), and the store still reloads on every event but raises the external-change toast only for real changes.

## What happened

Landed as briefed: is_presence extracted in lib.rs and reused by is_ignored, the OR-across-batch debounce keeps a mixed batch counting as real, HostClient.watch's callback signature updated along with the fake host. Verified by reading the diff and re-running suites; cargo check clean. Runtime behaviour is inferred from the logic and types since no live Tauri window was driven this session.

## Open questions

- Presence-only batches still trigger a full snapshot reload per debounce window (host spawn plus project parse); fine at this project's size, worth a throttle if large projects ever feel it.
