---
id: S-0111
ticket: T-0120
actor: claude
started: 2026-07-12T09:06:10Z
ended: 2026-07-12T09:06:10Z
commits: []
outcome: completed
---

## Approach

Programmer subagent, audited by the session model. Generalised the live orbit ring to one grouped rule (.ticket-card.live::after, .list-row.live::after) with border-radius: inherit so each species keeps its own radius, extended the reduced-motion static ring to both, and gave the ticket detail header a live readout: a sans cyan span (elapsed time and ticket id) in the view-header's right-hand slot, driven by the shared presence map passed through ProjectView.

## What happened

Landed as briefed with no duplicated gradient, mask or keyframes (verified by grep, exactly one definition). List rows already carried position relative and the seat radius, so the ring needed no positioning shim, and no fill was added since rows rest on the lane. Two new TicketDetail tests and one List test, all red-first proven. Suites green: desktop 255/255.

## Open questions

- The Graph view still shows no live signal; deliberate non-goal, ticket it if wanted.
- The header readout relies on the shared view-header right-hand-slot rule (app.css:781); narrowing that selector would regress its placement along with every other view using it.
