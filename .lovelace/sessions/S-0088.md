---
id: S-0088
ticket: T-0097
actor: claude
started: 2026-07-12T03:49:16Z
ended: 2026-07-12T03:49:16Z
commits: []
outcome: completed
---

## Approach

Reused the delete confirmation modal that already lives in ProjectView rather than copying it: TicketDetail gained an optional onRequestDelete prop and a header TrashIcon button matching the back button species (btn btn-secondary btn-icon), and ProjectView passes onRequestDelete={setDeleting} exactly as Board and List already do. The existing confirm handler already deletes through the host and closes the detail view, so the modal, copy and post-delete behaviour are shared byte-for-byte with the board and list pages.

## What happened

One programmer subagent implemented it; two unit tests in ticketDetail.spec.tsx (button present and called with the ticket when the prop is given, absent otherwise) and two ProjectView integration tests in board.spec.tsx (confirm deletes T-0002 via the host and closes the detail view; cancel makes no host call and stays open). Mid-run the programmer saw packages/core mutate.spec failures and flagged them; I verified they were a transient mid-edit state of a concurrent session in-flight core refactor (mutate.ts and mutate.spec.ts were being edited at the time) and the suite was green again minutes later. Final pnpm test and pnpm build run by me: all green.

## Open questions

- Work is uncommitted, in in_review for the owner visual pass.
- The header delete button and the modal confirm button share the accessible name Delete ticket; tests scope lookups to the modal. If that ambiguity bothers screen-reader use, the header button could take a more specific aria-label.
