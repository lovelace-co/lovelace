---
id: S-0106
ticket: T-0079
actor: claude
started: 2026-07-12T08:35:38Z
ended: 2026-07-12T08:35:38Z
commits: []
outcome: completed
---

## Approach

Complete T-0079 after the first verifier pass: extend the original-anchored rebuild to the two pre-existing neighbours the verifier found (authored redundant plural destroyed on save, unknown keys poisoning dirty detection), audit, and re-gate with the same verifier and its harness.

## What happened

The fix round added rebuildPlural (the rebuildLabel untouched/edited gate applied to plural, seeded against plural's human-label default to mirror the existing asymmetry) and re-based dirty detection on the seed-then-build projection of the loaded schema, so both sides of the comparison share one canon and unknown keys stop lighting Save. Red-first runs proved the two fixes independently necessary. The verifier re-confirmed with its original repros plus new gate attacks: authored plural clean on open and byte-preserved through no-edit and unrelated saves, persisting through a type rename; the labelled-type asymmetry no longer synthesises a phantom plural; the unknown-key fixture clean on open, byte-safe on save, with real edits still flipping dirty both ways; the combined hostile fixture (plural, unknown key, redundant labels, required false, typed defaults) clean on open. All three original acceptance criteria were confirmed in the first pass: non-enum defaults, redundant labels and explicit required false round-trip byte-faithfully (core compactField now emits required whenever defined), and a field renamed to title stays visible with core's duplicate rejection surfacing on save. Full suite green (core 190, desktop 251, mcp 81), build green.

## Open questions

- Authored label of an empty string is still silently deleted at serialisation by core's truthiness gates; pathological hand-edit, low severity, accepted and restated so it is not lost.
- The reseed-on-external-change gate deliberately keys off the raw snapshot while dirty uses the projection; correct division, worth remembering when either changes.
- apps/desktop/test/update.spec.tsx (the concurrent session's presence rework) is flaky under full-suite load; belongs to that workstream.
