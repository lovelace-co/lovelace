---
id: S-0120
ticket: T-0127
actor: claude
started: 2026-07-16T03:06:48Z
ended: 2026-07-16T03:06:48Z
commits:
  - c47d06e
outcome: completed
---

## Approach

Resumed T-0127 after clarifying its truncated body with the user (session S-0119): show release notes on the pre-migration SpecGate screen, sourced from SPEC.md's changelog, for major-version migrations only. Traced the existing migration architecture (ADR-0011, packages/core/src/migrations/registry.ts and types.ts, apps/desktop/src/components/SpecGate.tsx, and the mcp host/app IPC passthrough) to build an exact file map, resolved a real technical constraint with the user (the shipped app cannot read this repo's own SPEC.md at runtime, since it operates on a user's project), then briefed the programmer subagent with the confirmed scope, exact current shapes, non-goals and required tests.

## What happened

The programmer delivered and committed the change (c47d06e): a new packages/core/src/changelog.ts module with a structured SPEC_CHANGELOG constant and a changelogBetween(declared, target) filter, a sync test that parses SPEC.md's changelog section and asserts it matches the constant byte-for-byte, planProjectMigration widened to return releaseNotes, the app's MigrationPlan type widened to match, and SpecGate.tsx rendering a new 'What's changing' section before the technical per-step plan. Audited independently rather than trusting the report: read the full diff via git show, confirmed panel-heading was an existing class (not fabricated), and re-ran tests myself. apps/desktop was fully green (321/321). packages/core showed 14 failures in the working directory; rather than accept the report's own (methodologically flawed) stash-based baseline claim, built a real pre-T-0127 baseline in a separate git worktree, found only 9 pre-existing failures there, then traced the 5 extra failures to untracked, gitignored pollution in examples/demo-project/.lovelace/state/ (leftover presence/counter files from earlier local use of this checkout) by moving that directory aside and re-running: failures dropped to the same 9 pre-existing ones plus this ticket's 6 new passing tests (197 passed), conclusively ruling out a regression from this change. Plan mode activated unexpectedly mid-audit with no new task attached; confirmed with the user via AskUserQuestion that the only remaining step was closing out the ticket, then wrote a short administrative plan and exited plan mode to proceed.

## Open questions

- examples/demo-project/.lovelace/state/ in this local checkout carries leftover presence/counter files that make some unrelated packages/core tests (mutate.spec.ts, order.spec.ts, settings.spec.ts) fail locally; it's gitignored derived state, not a code bug, but worth clearing locally if it keeps causing confusion in future test runs.
