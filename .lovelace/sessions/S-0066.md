---
id: S-0066
ticket: T-0069
actor: claude
started: 2026-07-05T09:39:04Z
ended: 2026-07-05T09:39:04Z
commits:
  - b69ca86
outcome: completed
---

## Approach

Replaced the presence inference engine with the real signal: the agent helper gained presence-start and presence-clear commands, registered on UserPromptSubmit, Stop and SessionEnd by install_claude, writing and removing the machine-local state/presence.json marker (ticket, actor, started_at). Core gained writePresence/readPresence/clearPresence beside the active ticket pointer, the snapshot carries the marker, and the app derives liveness as a plain window: marker exists and is younger than the stale cap. The cap defaults to 120 minutes and is a new optional manifest key, presence_timeout_minutes, editable from Settings General through writeManifest with validation. The spec is bumped to 2.1.0 recording both additions, ADR-0009 records the decision, and the entire energy, half-life and decay machinery is deleted with the UI untouched.

## What happened

Full workspace suite green with new specs for the marker round trip, the manifest setting, the helper commands and the hook registration; build and typecheck clean. Presence is a display signal throughout: the helper never blocks a turn, and a torn marker file reads as still rather than erroring. Two pre-existing validation issues remain, both from the user-created Test.md scratch document.

## Open questions

- Existing projects pick up the new hooks the next time the Claude integration install is run; this repository itself has no .claude/settings.json, so its own sessions will not light the thread until that is installed.
