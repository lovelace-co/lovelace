---
id: S-0082
ticket: T-0087
actor: claude
started: 2026-07-12T03:14:30Z
ended: 2026-07-12T03:14:30Z
commits: []
outcome: completed
---

## Approach

Fix search so tickets are found by partial ticket ID, and surface the Cmd/Ctrl+K shortcut at the bottom of the search palette. Core matching lived in packages/core/src/search.ts where ticket IDs only matched by exact case-insensitive equality.

## What happened

search.ts now scores an exact ID match 5 and a substring ID match 3, above body hits (1) and below nothing that matters; snippet falls back to the ticket title for ID-only matches as before. Three new core tests cover partial ID, exact-beats-partial ranking and the title snippet fallback (116 core tests green). Added apps/desktop/src/lib/platform.ts exporting searchShortcutLabel (⌘K on macOS, ctrl+K elsewhere); SearchPalette palette-foot gained a fourth hint span reading like ⌘K opens this search, and the sidebar search button tooltip now uses the same label instead of hard-coded ⌘K. New searchPalette.spec.tsx asserts the hint renders. Verified end to end through the rebuilt MCP server: query 0086 returns T-0086, query T-008 returns all T-008x tickets ranked above session-body mentions. Full suite 367 tests and pnpm build green, run by the auditor. Work left uncommitted for the owner visual pass, same as T-0086 this session (S-0081).

## Open questions

- Work is uncommitted, in in_review for the owner visual pass.
- ID substring matching was scoped to tickets; documents and sessions are still found only through summary and body text, so a query like S-0078 relies on body mentions. Extending ID matching to those kinds is a small follow-up if wanted.
- The programmer used git stash/pop for a red-first test check despite the brief forbidding state-changing git commands; no harm resulted (stash list empty, tree intact) but briefs now need an explicit no-stash line.
