---
id: S-0056
ticket: T-0059
actor: claude
started: 2026-07-04T21:57:24Z
ended: 2026-07-04T21:57:24Z
commits: []
outcome: completed
---

## Approach

Phase 1 of E-0003 (ADR-0007). Added the two validated core mutations the Settings home needs, following the writeWorkflow / writeAutomations node-surgery pattern, then wired the host op and the app host client. No UI in this phase, so nothing visible changes.

## What happened

Added writeManifest (renames the project by patching the manifest.yaml name node in place, preserving other keys and comments) and writeActors (validates exactly one human, unique lowercase-slug ids and a name per actor, and blocks removing an actor still referenced by a ticket assignee, session or comment). Exposed write_manifest and write_actors host ops; added writeManifest and writeActors to HostClient, TauriHost and FakeHost. 9 new core tests. Core and mcp build clean, 109 core tests pass; app tsc clean, 136 app tests pass.

## Open questions

- Phases 3-4 (Settings shell and the sidebar flip) must conform to the Air design system (ADR-0006) and the rewritten CLAUDE.md design laws, both being edited concurrently; hold the UI phases until those settle.
- Actor id rename stays out of scope (it would need a cascade into tickets, sessions and comments); the Team editor will do add, rename-display-name, and remove-if-unreferenced only.
