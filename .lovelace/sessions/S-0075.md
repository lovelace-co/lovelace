---
id: S-0075
ticket: T-0082
actor: claude
started: 2026-07-12T02:05:05Z
ended: 2026-07-12T02:05:05Z
commits: []
outcome: completed
---

## Approach

Move the Sessions surface off the sidebar into Settings > General as a button opening a modal, following the digest modal pattern from T-0073. Scouted the existing Sessions view, modal chrome and tests, then briefed the programmer agent with exact files and conventions.

## What happened

New SessionsModal component carries the session list unchanged (reverse-chronological rows, lazy body fetch, expand and collapse, empty state) inside the shared modal chrome with a new records-card and records-well surface copied from the digest card. GeneralSettings gains a View sessions button with a count note; clicking a session ticket closes the modal and opens the ticket. The Activity nav group is gone and a session search hit now opens that session ticket, falling back to Settings. The old Sessions view file is deleted. Also removed, at the owner request mid-session, the ID numbering example line from the Types page at rest; the example survives inside the Edit type modal where it serves the prefix input. All suites and the build are green.

## Open questions

- Work is uncommitted, in in_review for the owner visual pass, on top of the large uncommitted schema rename tree.
- The sidebar no longer surfaces the problems count passively; it now lives behind Settings > General. Worth confirming that trade-off in daily use.
- During the session a stale editor buffer repeatedly overwrote TypesFieldsSection.tsx and views.spec.tsx, restoring the removed ID example. If it reappears, close the stale editor tab for those files and remove the lines again.
