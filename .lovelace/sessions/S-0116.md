---
id: S-0116
ticket: T-0124
actor: claude
started: 2026-07-12T10:41:47Z
ended: 2026-07-12T10:41:47Z
commits:
  - b2e06ed
outcome: completed
---

## Approach

Programmer subagent briefed to reproduce red-first and fix the init scalar bug caught by release run 29187958890; my audit probed boundary values, and the fresh-context verifier attacked byte-stability and read-back before commit.

## What happened

Root cause corrected during implementation: project_id is random hex (CI drew an all-digit value, roughly a 1-in-285 event per init), while the deterministic user-facing vector is the manifest name, prefilled from the folder basename, and the actors.yaml user name; all three were spliced into hand-assembled YAML unquoted. Fix in commit b2e06ed: a yamlScalar helper in packages/core/src/init.ts quotes a scalar only when its plain form would not round-trip as the same string, multiline values become one-line double-quoted JSON strings, and blank name or userName is refused up front instead of scaffolding a project that can never load (the last two close verifier findings F1 and F2). Regression test inits a folder literally named 2048 through the host op. Verifier confirmed byte-identical output for unambiguous names against the pre-change code and exact read-back for 36 adversarial names; boundary values (007, 1e3, null, true, 0x1A, ": ", "#", empty) all round-trip or are refused clearly. Full serial suite 538 green; the hand-edited unquoted numeric project_id still fails validation with the existing message.

## Open questions

- None for the fix itself. The macOS release job that surfaced this should go green on the next tag run; confirmed only then.
