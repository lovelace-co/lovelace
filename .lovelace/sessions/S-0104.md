---
id: S-0104
ticket: T-0113
actor: claude
started: 2026-07-12T08:22:51Z
ended: 2026-07-12T08:22:51Z
commits: []
outcome: completed
---

## Approach

Put the block editor placeholder exactly where typed text appears. Scout diagnosed the offset: the placeholder was an absolute overlay at a hardcoded top 64px and left 42px beside the scroller, with the browser default line-height instead of the content's 1.6. A programmer agent implemented the structural fix from a brief; I audited and extended it to the two contexts the brief scoped out.

## What happened

BlockEditor now wraps RichTextPlugin in the scroller div (ref and class unchanged) so Lexical renders the placeholder as a sibling of the editable inside .lexical-scroller. The placeholder rule dropped the toolbar-dependent 64px for top 14px, matching the content's own top padding, and gained line-height 1.6 to match typed text; left 42px equals the scroller's 26px rail plus the content's 16px inset, and the first paragraph carries no top margin, so the overlap is glyph-exact in the documents editor. A red-first-proven structural test asserts the placeholder renders inside the scroller. The programmer flagged that the ticket body and comment editors zero the scroller rail, leaving the old 26px misalignment there; since the ticket asks for exact placement everywhere I added a matching override (left 16px for .body-editor and .comment-editor placeholders). Desktop suite 244/244 and desktop tsc clean. Alignment was verified by box-model reasoning and structure tests, not pixel measurement, since jsdom does not lay out.

## Open questions

- A concurrent session is mid-refactor on presence and schema config: at last check pnpm build fails in core (schema-config.ts and migrations/v2-to-v3.ts missing exports from config.js) and four mcp helper.spec.ts presence tests fail; none of it touches this ticket's files.
- Placeholder alignment holds by construction; a quick visual pass in the running app across documents, ticket body and comment editors would close the loop.
- Nothing committed: the tree still stacks the schema rename and prior tickets; the owner decides when to land it.
