---
id: S-0117
ticket: T-0125
actor: claude
started: 2026-07-12T23:46:00Z
ended: 2026-07-12T23:46:00Z
commits:
  - 2d892d9
  - b799903
  - "4238698"
outcome: completed
---

## Approach

Continued from S-0115: land the CRLF fixes, then iterate the remaining Windows failures to zero on the tests-only workflow, evidence first at every step.

## What happened

The Windows suite went 85 failures to 3 to 2 to 1 to 0 across four validation runs, each survivor a genuinely new discovery unmasked by the previous fix. After the CRLF fixes (b2e06ed): a separator-sensitive test assertion on addComment's native return path (fixed in 2d892d9); the watcher double-flush, which a branch-only instrumented run proved was the watcher reacting to its own index.json write because chokidar hands the ignored callback non-native separators on Windows so the join()-built prefix checks never matched (fixed in b799903 by comparing forward-slash forms; the earlier awaitWriteFinish guess was reverted as actively harmful); and a POSIX-only executable-bit assertion in the mcp git hook test, the first time mcp ever ran on Windows (fixed in 4238698). Validation run 29190260772: full suite green on windows-latest and ubuntu-22.04. The final release run 29213229096 then passed all four platforms. Root causes are recorded on this ticket and in the commits; .gitattributes and the test.yml workflow are permanent. The ci/t-0125-windows-validation branch and its diagnostic instrumentation were deleted after use.

## Open questions

- extractSection still splits on LF only, so digest snippets from CRLF bodies would keep interior carriage returns; cosmetic, unticketed.
- The tests-only workflow runs on ci/** pushes and manual dispatch only; consider running it on pull requests if the project ever takes contributions.
