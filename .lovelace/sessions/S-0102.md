---
id: S-0102
ticket: T-0111
actor: claude
started: 2026-07-12T08:16:31Z
ended: 2026-07-12T08:16:31Z
commits: []
outcome: completed
---

## Approach

Make sessions first-class in search and deep-link a clicked session hit into the sessions modal on the settings page. Scout mapped the search pipeline and session rendering; a programmer agent implemented from a brief; I audited the diff, re-ran the scoped suites and added a one-line remount fix.

## What happened

Core search already indexed session bodies; sessions now also match on their ID with the same scoring as tickets (exact 5, partial 3, body 1, summed into one hit), covered by three new red-first-proven tests in misc.spec.ts. In the app, picking a session hit no longer opens its ticket: ProjectView records a session focus and navigates to Settings, which switches to the General tab and passes the focus to GeneralSettings; that opens SessionsModal with a new initialOpen prop which pre-expands the session, loads its body through the existing effect, and scrolls the row into view (scrollIntoView optional-chained for jsdom). The focus is consumed on open and cleared on close and on manual opens, so repeat picks work. On audit I keyed SessionsModal on the focused session so a pick made while the modal is already open remounts and re-targets it. Two new desktop tests cover pre-expansion and the no-match no-op. Scoped verification green: core misc.spec 11/11, desktop suite 243/243, tsc clean for core and desktop. Full pnpm test and build were green at 18:08 but a concurrent session in this same checkout began a presence refactor at 18:09 (readPresence renamed to readPresences in core mutate.ts and index.ts) which currently fails settings.spec.ts in core and three mcp tests; that work is not part of this ticket and was deliberately left untouched.

## Open questions

- A concurrent session is refactoring presence in this same checkout; at last check core settings.spec.ts and three mcp tests fail on the readPresence to readPresences rename. Whoever owns that ticket needs to finish or revert it before the tree is green.
- Session id search scoring copied the ticket convention (5 exact, 3 partial, 1 body); revisit if sessions should rank below tickets on equal matches.
- Nothing committed: the tree still stacks the schema rename and prior tickets; the owner decides when to land it.
