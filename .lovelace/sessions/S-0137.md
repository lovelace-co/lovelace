---
id: S-0137
ticket: T-0139
actor: claude
started: 2026-07-21T08:44:15Z
ended: 2026-07-21T08:44:15Z
commits:
  - 9edb73f
  - 8a5ba34
  - e96449c
outcome: completed
---

## Approach

Wire the user's Apple Developer credentials into the release pipeline and prove a fully signed, notarised four-platform release: verify the exported .p12 by keychain import, set the six APPLE_* secrets, then cycle the v0.1.0 tag until green, fixing whatever the run surfaced.

## What happened

Certificate verified (Developer ID Application: ADACA PROJECTS PTY LTD, team 6MAJR49B5T) and all six secrets set. Three release attempts: the first proved signing end to end (HAS_APPLE_SIGNING=true, notarisation Accepted) but Windows failed the version guard because sync-version.mjs parsed a CRLF Cargo.lock, the one version site .gitattributes did not pin; fixed by pinning Cargo.lock to LF and making the script CR-tolerant, with a local red/green proof. The second attempt failed the Windows mcp suite: the fixture-copy filter in packages/mcp/test/helpers.ts split paths on forward slash while cpSync hands native separators, so the fixture's newly tracked .claude assets leaked into temp fixtures and broke hook idempotency; fixed with node:path basename and validated on the cheap test.yml Windows runner before re-tagging. Third run green on all five jobs. Verified the shipped artefact locally: codesign strict deep verify valid, spctl accepted with source Notarized Developer ID. Stapler validate fails with error 68 (ticket not attached, online-only verification); filed T-0141 for stapling. Also en route: closed T-0130 via a programmer subagent (session S-0136) and hit the stale-counter overwrite bug, filed as T-0140.

## Open questions

- Windows code signing still needs a decision: Azure Trusted Signing (recommended, needs workflow rework) or a cloud HSM certificate.
- Publishing the draft release and the T-0117 staged-publishing flow remain before anything is user-downloadable.
