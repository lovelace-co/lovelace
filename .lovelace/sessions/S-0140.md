---
id: S-0140
ticket: T-0143
actor: claude
started: 2026-07-22T07:13:24Z
ended: 2026-07-22T07:13:24Z
commits:
  - "06141e2"
outcome: completed
---

## Approach

Fix the Windows hook failure by changing how install_claude composes command strings: normalise backslash paths to forward slashes centrally in installClaudeAssets, quote hook command paths only when they contain a space, parse the bare-binary and node-script command shapes explicitly instead of splitting on every space, and migrate stale-format Lovelace hook entries on reinstall. Implementation delegated to the programmer subagent against a settled design; audited by reading the diff, re-running checks, and empirically testing the bash contract.

## What happened

Implemented in packages/mcp/src/claude.ts only: parseCommand handles the two command shapes (bare path, possibly with spaces, and node script), writeMcpJson no longer mangles spaced paths and keeps .mcp.json unquoted, writeHooks quotes the helper path only when spaced and prunes stale Lovelace entries (identified by helper reference plus subcommand suffix, never matching user hooks) before the ensure calls, and installClaudeAssets normalises both commands to forward slashes. Five new tests in packages/mcp/test/claude.spec.ts pin exact command strings for the spaced and unspaced Windows shapes, the node dev shape, migration with a user hook preserved, and idempotency. Full workspace pnpm test passed (637 passed, 26 skipped) and pnpm build passed; POSIX output verified byte-identical via the pre-existing tests. Empirically confirmed against real bash that the old backslash form reproduces command not found while the new quoted forward-slash form executes with its argument intact. Also resolved ticket housekeeping: cancelled T-0144 as a duplicate of T-0140 and folded the 2026-07-22 recurrence details into T-0140.

## Open questions

- Confirm on the actual Windows machine: rebuild the sidecars, reinstall the app, re-run the Claude Code install from Lovelace, and check the hooks fire; the ticket sits in_review until then.
- Existing broken Windows projects heal on reinstall via the new migration, but only if the user re-runs the install from the app; is that discoverable enough or should the app detect stale hook formats?
